# Authentication API Configuration

# Server Configuration
PORT=3002
HOST=0.0.0.0
LOG_LEVEL=info

# Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# Service URLs
DEPLOYMENT_ENGINE_URL=https://deploy.noderr.xyz
AUTH_API_URL=https://auth.noderr.xyz
DOCKER_REGISTRY=ghcr.io/noderrxyz
TELEMETRY_ENDPOINT=https://telemetry.noderr.xyz

# Security (REQUIRED)
# Generate with: openssl rand -hex 64
JWT_SECRET=

JWT_EXPIRY_HOURS=24
CREDENTIAL_EXPIRY_DAYS=365
TOKEN_EXPIRY_DAYS=7

# Node Version (used by heartbeat to trigger auto-updates)
LATEST_VERSION=1.0.0

# Bootstrap Nodes for P2P peer discovery (comma-separated multiaddrs)
# Deploy 3-5 bootstrap nodes on stable public IPs and list them here.
# Example: /ip4/1.2.3.4/tcp/4001/p2p/QmPeer1,/ip4/5.6.7.8/tcp/4001/p2p/QmPeer2
BOOTSTRAP_NODES=

# Oracle Consensus (required if Oracle nodes are deployed)
ORACLE_VERIFIER_ADDRESS=
RPC_URL=https://sepolia.base.org

# Admin API key for token generation and management endpoints
# Generate with: openssl rand -hex 32
ADMIN_API_KEY=

# Email notifications via Resend (optional — degrades gracefully if not set)
RESEND_API_KEY=
EMAIL_FROM=noreply@noderr.xyz

# Typeform webhook signature verification (optional)
TYPEFORM_WEBHOOK_SECRET=

# Staking verification
NODR_TOKEN_ADDRESS=

# Optional: Redis for distributed rate limiting
# REDIS_URL=redis://localhost:6379
